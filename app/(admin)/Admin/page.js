export const metadata = {
  title: "Admin Dashboard | Sodra",
  description: "Manage products, orders, and users from the admin panel.",
};

import DashboardPage from './DashboardPage'

const Admin = () => {
  return (
    <div>
      <DashboardPage/>
    </div>
  )
}

export default Admin
