export const metadata = {
  title: "Logged Out | Sodra",
  description: "You have been successfully logged out of your account.",
};

import LogoutContent from "./LogoutContent";

const Logout = () => {
  return (
    <div>
      <LogoutContent/>
    </div>
  )
}

export default Logout
