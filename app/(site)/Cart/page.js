export const metadata = {
  title: "Your Shopping Cart | Sodra",
  description: "Review items in your cart and proceed to secure checkout.",
};

import CartContent from "./CartContent"

const Cart = () => {
  return (
    <div>
      <CartContent/>
    </div>
  )
}

export default Cart
